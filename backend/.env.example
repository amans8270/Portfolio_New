# ─────────────────────────────────────────────────────────────────────────────
# Aman Singh Portfolio — Backend Environment Variables
# Copy this file to .env and fill in your actual values.
# Instructions provided at the end of the project setup.
# ─────────────────────────────────────────────────────────────────────────────

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database name
DB_NAME=portfolio

# JWT — generate a secure random string: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your-super-secret-key-here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# SMTP (Gmail recommended — use App Password, NOT your account password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail@gmail.com
SMTP_PASSWORD=your-gmail-app-password
CONTACT_RECIPIENT=your-gmail@gmail.com

# OpenAI API key (for LangChain RAG chatbot)
OPENAI_API_KEY=sk-...

# CORS — comma-separated list of allowed origins
# Development: http://localhost:3000
# Production: https://your-vercel-app.vercel.app
ALLOWED_ORIGINS=http://localhost:3000

# File upload directory (relative path)
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE_MB=5
